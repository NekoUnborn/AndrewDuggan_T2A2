<%= form_with url: add_role_path, method: :post do |f| %>
  <div class="field">
    <%= f.label :role %>
    <%= f.collection_select :role_id, @roles, :id, :name %>
  </div>
  <div>
    <%= f.submit "Chosen Role" %>
  </div>
<% end %>
